Add your CV (PDF) to the site root as `mayowa-cv.pdf`.

Recommended steps:

1. Create a PDF resume named `mayowa-cv.pdf`.
2. Place it in the site root next to `index.html`.
3. The homepage buttons (`Download CV`) link to `/mayowa-cv.pdf` and will prompt download.

Optional optimizations:
- Use an A4, single-column layout with clear headings.
- Include contact email `hello@mayowa-kalejaiye.vercel.app` at top of CV.
- Compress PDF to <200KB for faster downloads.

If you want, I can generate a simple one-page PDF from your README content — say the word and I'll create it as `mayowa-cv.pdf`.
